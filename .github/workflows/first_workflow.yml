name: first_workflow
on: workflow_dispatch
jobs:
   first-job:
      runs-on: ubuntu-latest
      steps: 
         - name: print greetings
           run: echo"Hello-guy!"
         - name: print good bye
           run: echo"Done-yet!"
